# Multimodal Agentic AI — Finance Automation
# Core stack: Claude orchestrates, Qwen handles vision, Databricks provides data/ML

# ─── Anthropic (Claude Orchestrator + Intent Router) ───────────────────────
anthropic>=0.40.0
claude-agent-sdk>=0.1.0

# ─── Async execution (Kimi PARL parallel orchestration) ────────────────────
anyio>=4.0.0
asyncio-throttle>=1.0.2

# ─── Databricks (Lakehouse + MLflow audit trail) ────────────────────────────
databricks-sql-connector>=3.0.0
mlflow>=2.10.0
databricks-sdk>=0.20.0

# ─── Document processing (multimodal inputs) ────────────────────────────────
pillow>=10.0.0          # Image preprocessing
pypdf>=4.0.0            # PDF extraction
openpyxl>=3.1.0         # Excel file reading
python-docx>=1.1.0      # Word document extraction

# ─── Data and utilities ─────────────────────────────────────────────────────
pydantic>=2.0.0         # Structured outputs validation
pyyaml>=6.0             # Agent YAML frontmatter parsing
python-dotenv>=1.0.0    # Environment variable management
httpx>=0.27.0           # Async HTTP (for API calls)
tenacity>=8.2.0         # Retry logic with exponential backoff

# ─── Development and testing ────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
